## How to run with nest-cli:
on individual terminals:
- cd backend && nest start gateway --watch
- cd backend && nest start user --watch

## How to use docker:
- cd backend && docker-compose up --build